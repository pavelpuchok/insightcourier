version: "2"
servers: []
sql:
  - engine: postgresql
    queries: ./db/queries/
    schema: ./db/migrations
    gen:
      go:
        package: psql
        out: ./storage/psql/
        sql_package: pgx/v5
